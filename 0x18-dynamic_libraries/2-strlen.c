#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"

/**
 * _strlen - check the code for Alx school students.
 * @s: argument of function.
 *
 * Return: Always 0 (Success)
 */

int _strlen(char *s)

{
int i;

for (i = 0; s[i] != 0; i++)
{
}
return (i);
}
